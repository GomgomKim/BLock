<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".tab.AcceptFragment"
    android:background="#000000"
    android:paddingRight="30dp"
    android:paddingLeft="30dp"
    >

    <TextView
        android:id="@+id/title_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/none_block_img"
        android:layout_marginTop="50dp"
        android:fontFamily="@font/bodoniflf_bold"
        android:text="Request"
        android:textColor="#858edb"
        android:textSize="25dp"></TextView>

    <View
        android:id="@+id/span_line"
        android:layout_below="@+id/title_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="#858edb"
        >
    </View>

    <ScrollView
        android:layout_below="@+id/span_line"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="50dp"
        >
        <GridLayout
            android:id="@+id/request_grid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="1"
            android:orientation="horizontal"
            android:layout_gravity="fill"
            ></GridLayout>
    </ScrollView>

</RelativeLayout>